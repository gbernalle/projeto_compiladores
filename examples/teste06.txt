program
int : x, y, z;
float : media;
char : letra;

begin
    x = 10;
    y = 20;
    z = x + y;
    media = (x + y + z) / 3.0; 
    
    if media >= 15 then
        out("Aprovado")
    else
        out("Reprovado")
    end; 

    repeat
        in(x);
        x = x - 1
    until x <= 0 
    
end